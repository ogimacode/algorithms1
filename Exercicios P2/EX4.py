# Faça uma função que cria uma lista, ou seja, retorna uma lista vazia. Faça a chamada no programa principal.
def cria_lista(lista):
    lista = []
    return lista

lista = 0
lista_criada = cria_lista(lista)
print(lista_criada)